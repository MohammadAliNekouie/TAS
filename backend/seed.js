// Sample data is seeded automatically the first time db.js opens an empty
// database. This script exists so you can run `npm run seed` explicitly,
// e.g. after deleting backend/data/accounting.db to reset to fresh demo data.
require("./db");
console.log("Database is ready at backend/data/accounting.db");
