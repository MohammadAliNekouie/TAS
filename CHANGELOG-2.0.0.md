# TAS Accounting App 2.0.0

این نسخه چرخه‌های اصلی حسابداری، انبار، خزانه و کنترل مالی را یکپارچه می‌کند.

## موارد اصلی
- دفتر دوطرفه با کنترل دقیق تراز و مبالغ صحیح ریالی
- Inventory Ledger و بازسازی Moving Average پس از تغییرات تاریخی
- بازسازی خودکار COGS تمام فاکتورهای فروش پس از تغییر خرید/موجودی
- اتصال خرید/فروش، دریافت/پرداخت، چک، تنخواه، تعدیل و تولید به سند حسابداری
- مالیات و تخفیف فاکتور
- برگشت فاکتور خرید/فروش با کنترل فاکتور اصلی و سقف مقدار برگشت
- دوره مالی، جلوگیری از ثبت در دوره بسته و سند اختتامیه
- Audit Log و گزارش رویدادهای مدیر سیستم
- دفتر کل، سود ناخالص، جریان نقدی، گردش طرف حساب و گردش موجودی
- Backup/Restore با نسخه امن قبل از Restore و اعتبارسنجی ساختار فایل
- حساب مستقل خودکار برای حساب‌های بانکی
- CORS محدود و Rate Limit ورود
- JWT فقط در HttpOnly Cookie؛ توکن دیگر در localStorage نگهداری نمی‌شود
- Transaction و rollback در عملیات حساس

## تست‌های انجام‌شده
- بررسی syntax تمام فایل‌های JavaScript بک‌اند با `node --check`
- بررسی اجرای کامل schema در SQLite استاندارد
- بررسی integrity ساختار ZIP قبل از تحویل

> در محیط ساخت این نسخه، نصب وابستگی‌های native (`better-sqlite3`) و build کامل Vite به‌دلیل timeout محیط اجرا تکمیل نشد؛ بنابراین تست end-to-end مرورگر و اجرای واقعی دیتابیس با dependency نصب‌شده در این محیط انجام نشده است.
