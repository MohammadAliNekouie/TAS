import React,{useEffect,useState} from 'react';import {auditLogApi} from '../lib/api.js';export default function AuditLog(){const [rows,setRows]=useState(null);useEffect(()=>{auditLogApi.list().then(setRows).catch(()=>setRows([]))},[]);return <div style={{padding:28}}><h1 style={{fontSize:20,margin:0}}>گزارش رویدادهای سیستم</h1><p style={{fontSize:13,color:'var(--ink-soft)'}}>ثبت عملیات تغییر‌دهنده داده برای بررسی و ممیزی.</p><div style={{background:'var(--surface)',border:'1px solid var(--border)',borderRadius:14,overflow:'auto'}}>{!rows?<div style={{padding:20}}>در حال بارگذاری...</div>:<table style={{width:'100%',borderCollapse:'collapse',fontSize:12}}><thead><tr style={{background:'var(--bg)',textAlign:'right'}}><th style={{padding:10}}>زمان</th><th>کاربر</th><th>عملیات</th><th>موجودیت</th><th>جزئیات</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} style={{borderTop:'1px solid var(--border)'}}><td style={{padding:10}}>{r.created_at}</td><td>{r.full_name||r.username||'—'}</td><td>{r.action}</td><td>{r.entity_type} {r.entity_id||''}</td><td style={{maxWidth:500,whiteSpace:'pre-wrap'}}>{r.details||''}</td></tr>)}</tbody></table>}</div></div>}
